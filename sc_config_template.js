const oauth_token = "";
const client_id = "";
const user_id = "";
const datadome_clientid = "";

if (typeof module !== "undefined" && module.exports) {
  module.exports = {
    oauth_token,
    client_id,
    user_id,
    datadome_clientid,
  };
}
